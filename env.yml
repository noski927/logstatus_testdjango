# name: testdjango
name: app_env
prefix: /opt/conda/envs/app_env
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.12
  - django=5.2
  - celery=5.5.*
  - redis-py
  - python-dotenv
  - django-redis
  - pip
  - pip:
      - django-redis
